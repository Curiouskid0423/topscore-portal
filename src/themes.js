import { createMuiTheme } from "@material-ui/core/styles";

const themehelper = createMuiTheme({
    palette: {
        primary: {
            main: "#BD3A3A",
            light: "#F78A8A"
        },
        secondary: {
            main: "#5B3330"
        }
    }
});

export default themehelper;